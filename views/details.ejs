<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Cat Details</title>
</head>
<body>
    <div class="container">
        <h1>Details Page for Cat <%= cat.name; %></h1>
        <img src="<%= cat.imagePath; %>" alt="<%= cat.name %> Picture">
        <h2>Favorite Sleeping Spot: <%= cat.sleepingSpot; %></h2>
        <h2>Age: <%= cat.age; %> year/s old</h2>
        <ul>
            <h2>Favorite Foods:</h2>
            <% for(let i = 0; i < cat.favoriteFoods.length; i++) { %>
                <li><%= cat.favoriteFoods[i]; %></li>
            <% } %>
        </ul>
    </div>
</body>
</html>